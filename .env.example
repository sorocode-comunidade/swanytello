# Database
# For local development with docker-compose:
# DATABASE_URL="postgresql://swanytello:swanytello_password@localhost:5432/swanytello?schema=public"
# Or use environment variables from docker-compose:
DATABASE_URL="postgresql://${POSTGRES_USER:-swanytello}:${POSTGRES_PASSWORD:-swanytello_password}@localhost:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-swanytello}?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_ACCESS_EXPIRATION="15m"

# Server
PORT=3000
HOST=0.0.0.0

# Authentication (ON = enforce JWT, OFF = bypass for development)
AUTH_STATUS=ON

# Node
NODE_ENV=development

# File storage (optional; default: project root / uploads)
# UPLOAD_DIR=/var/app/uploads

# RAG – Default LLM is Ollama Cloud. Override only if you want OpenAI (set OPENAI_API_KEY or RAG_LLM_PROVIDER=openai).
# If RAG_LLM_PROVIDER is unset and OPENAI_API_KEY is set → OpenAI is used. Otherwise Ollama Cloud (default).
# RAG_LLM_PROVIDER=ollama-cloud
# RAG_LLM_PROVIDER=openai

# Ollama Cloud (default): uses official ollama package, e.g. ollama.chat({ model, messages }). Change only if needed.
# OLLAMA_CLOUD_HOST="https://api.ollama.com"
# OLLAMA_CLOUD_MODEL="glm-4.7-flash"
# OLLAMA_API_KEY="your-ollama-cloud-api-key"

# When using OpenAI (RAG_LLM_PROVIDER=openai or just set OPENAI_API_KEY):
# OPENAI_API_KEY="sk-..."
# OPENAI_MODEL="gpt-4o-mini"
