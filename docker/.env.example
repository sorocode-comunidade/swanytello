# Docker Compose Environment Variables
# Copy this to .env in the project root or set these in your shell environment

# PostgreSQL Configuration
POSTGRES_USER=swanytello
POSTGRES_PASSWORD=swanytello_password
POSTGRES_DB=swanytello
POSTGRES_PORT=5432

# Ollama (RAG local LLM) Configuration
OLLAMA_PORT=11434
